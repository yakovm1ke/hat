import { Page } from '@/components/ui'
import { VueComponent, Component } from '@/types'
import { VNode } from 'vue'

@Component<StageEndView>({})

export class StageEndView extends VueComponent {

	public render(): VNode {
		return (
			<Page>

			</Page>
		)
	}
}
