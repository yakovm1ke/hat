import { Page } from '@/components/ui'
import { VueComponent, Component } from '@/types'
import { VNode } from 'vue'

@Component<MoveEndView>({})

export class MoveEndView extends VueComponent {

	public render(): VNode {
		return (
			<Page>

			</Page>
		)
	}
}
